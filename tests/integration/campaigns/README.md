# Protocol v2 integration tests

This directory contains bounded schema and lifecycle integration checks for the
v2 protocol. Candidate-restore and physical-archive schema contracts are covered;
restart, stage-gate, interleaving, and short end-to-end tests are added with
their runtime milestones. None may start the thesis campaign automatically.
